{
  "name": "ai-code-reviewer",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm:backend\" \"npm:frontend\" --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\"",
    "backend": "node backend/app.js",
    "frontend": "npm run dev --prefix frontend -- --port 5174"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
